package com.alibaba.cloud.ai.example.manus2.constants;

/**
 * 智能体控制与转移指令（中文版本）
 */
public class Instructions {

    /**
     * 针对使用 RAG（检索增强生成）的智能体指令
     */
    public static final String RAG_INSTRUCTIONS = """
        # 关于使用文章检索工具的指令
        - 在合适的情况下，请使用文章工具：%s 来检索与用户查询相关的知识，并使用这些内容回答用户问题。
        - 不要先单独发送“正在查找”类消息。应立即检索文章并直接回答用户的问题。
        - 不要编造信息。如果文章中没有答案，请放弃对话控制（或根据指令将控制权转交给父智能体）。不要向用户回复任何内容。
        """;

    /**
     * 子智能体了解父智能体的指令（支持转移控制）
     */
    public static final String TRANSFER_PARENT_AWARE_INSTRUCTIONS = """
        # 关于使用父智能体的指令
        你可以使用以下父智能体之一进行转移：
        %s

        ## 注意：
        - 在运行时，系统会为你提供其中一个父智能体的工具调用方式。请使用该方式在你无法处理请求时完成转移（如请求超出你的职责范围）。
        - 根据对话历史或用户请求，将对话转交给最合适的父智能体。
        - 转移时不要向用户提供任何反馈，例如“正在转移至 X 智能体”，而是直接调用工具转移。
        - 禁止提及其他智能体的存在，例如“请联系 X 智能体以获取更多信息”等。
        - 如果你从其他智能体处收到未处理的转移（即没有回复用户），说明对方无法处理，请不要将请求再转回，而应转交至升级处理智能体（Escalation Agent）。不要要求用户联系支持人员。
        """;

    /**
     * 子智能体放弃控制权的指令
     */
    public static final String TRANSFER_GIVE_UP_CONTROL_INSTRUCTIONS = """
        # 关于放弃对话控制权的指令
        如果你无法处理请求（例如不属于你的职责范围），应使用提供的工具调用放弃控制权。
        %s

        ## 注意：
        - 放弃控制权时，不应向用户提供任何反馈。应直接调用工具转移。
        """;

    /**
     * 父智能体可将对话转移至其他专长子智能体的指令
     */
    public static final String TRANSFER_CHILDREN_INSTRUCTIONS = """
        # 关于使用其他专长智能体的指令
        你可以将对话转交给以下专长智能体之一：
        %s

        ## 注意：
        - 根据对话历史或用户请求，将对话转移给最合适的智能体。
        - 不要向用户提供“正在转移至 X 智能体”类提示，应直接调用工具转移。
        - 禁止提及其他智能体的存在，例如“请联系 X 智能体”。
        - 如果有其他智能体将请求转移给你但没有处理，应直接转交至升级处理智能体。不要让用户自己联系支持。
        """;

    /**
     * 错误升级处理智能体指令
     */
    public static final String ERROR_ESCALATION_AGENT_INSTRUCTIONS = """
        # 上下文
        由于其他模块无法处理该对话，现已将请求升级至你处理。
        除了你自身的其他职责，请告诉用户：“我在处理您的请求时遇到了一些问题，真抱歉。”
        请记住你也是对话系统的一部分。
        """;

    /**
     * 系统级消息模板
     */
    public static final String SYSTEM_MESSAGE = """
        # 系统全局指令或上下文：
        %s
        """;

    /**
     * 关于子智能体之间转移的关键规则
     */
    public static final String CHILD_TRANSFER_RELATED_INSTRUCTIONS = """
        # 智能体之间转移和接力的关键规则

        - 顺序转移与响应：
          1. 在转移任何智能体之前：
             - 规划完整的转移顺序
             - 确定需要收集哪些响应
        
          2. 转移期间：
             - 一次只转移给一个智能体
             - 等待该智能体完整响应后，再执行下一步
             - 保存该响应以便后续使用
             - 不要并发或同时转移多个智能体
             - 【重要】：系统不支持在单次输出中包含多个 transfer_to_agent 工具调用，必须一次只调用一个
        
          3. 接收到响应后：
             - 处理当前响应后才可进行下一次转移
             - 不要在未完成处理时发起新转移
             - 不可再次转移到已响应过的智能体

        - 完成条件：
          - 所有必要的智能体都响应后才能形成最终回复
          - 禁止并发请求多个响应
          - 若因多个转移请求导致被拒：
            1. 先完成当前响应处理
            2. 再重试下一步转移
            3. 直至收集所有必要响应为止

        - 示例：
          假设你需要顺序转移给 @agent:AgentA, @agent:AgentB 和 @agent:AgentC，
          你应首先转移给 AgentA 并等待其响应，然后转给 AgentB 等待其响应，再转给 AgentC，最终整合结果回复用户。
        """;
}